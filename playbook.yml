---
- hosts: localhost
  gather_facts: false
  collections:
    - redhat.jcliff
  roles:
    - jcliff

  tasks:
    - jcliff:
        wfly_home: "{{ jboss_home }}"
        subsystems:
          - system_props:
              - name: jcliff.enabled
                value: 'enabled'
